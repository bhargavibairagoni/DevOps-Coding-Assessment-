apiVersion: v1
kind: Service
metadata:
name: budget-app-service
annotations:
service.beta.kubernetes.io/aws-load-balancer-type: "nlb"  # or remove for classic
spec:
selector:
app: budget-app
ports:
\- port: 80
targetPort: 3000
type: LoadBalancer
