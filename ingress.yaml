apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
name: budget-app-ingress
annotations:
kubernetes.io/ingress.class: alb
alb.ingress.kubernetes.io/scheme: internet-facing
spec:
rules:

* http:
  paths:

  * path: /
    pathType: Prefix
    backend:
    service:
    name: budget-app-service
    port:
    number: 80

\[ec2-user\@ip-172-31-93-18 \~]\$ 
